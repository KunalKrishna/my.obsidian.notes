
## Interview Questions

> [!question]- Why is `String` immutable in Java?
> String pool sharing, thread safety, hashcode caching for HashMap keys,
> security (class loading, network params can't be mutated after validation).
> **Follow-up asked at Redyhire:** what does `intern()` actually do?

> [!question]- StringBuilder vs StringBuffer — when does the difference matter?
> ...
> sd
> s
> s
> 