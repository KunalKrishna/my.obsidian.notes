# Java Concurrency Mastery Roadmap — SDE Interview Edition

---
## The Roadmap 

```
    ╔══════════════════════════════════════════════════════════════════════════════════════════════╗
    ║                JAVA CONCURRENCY MASTERY ROADMAP  ——  SDE INTERVIEW EDITION                   ║
    ║                            ( 10 - 12 Weeks Plan )                                            ║
    ╚══════════════════════════════════════════════════════════════════════════════════════════════╝

          WEEK 1            WEEK 2-3           WEEK 4-5          WEEK 6-7          WEEK 8-9        WEEK 10-12
            │                  │                  │                  │                 │                │
            ▼                  ▼                  ▼                  ▼                 ▼                ▼
   ┌─────────────────┐ ┌───────────────┐ ┌───────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐
   │   [ LEVEL 0 ]   │ │  [ LEVEL 1 ]  │ │  [ LEVEL 2 ]  │ │  [ LEVEL 3]  │ │  [ LEVEL 4 ] │ │   [ LEVEL 5 ]    │
   │  ─────────────  │ │ ────────────  │ │ ────────────  │ │ ───────────  │ │ ───────────  │ │  ─────────────   │
   │  PREREQUISITES  │ │  FOUNDATION   │ │    j.u.c      │ │   ADVANCED   │ │ SPRING BOOT  │ │ INTERVIEW READY  │
   │  & JVM BASICS   │ │  THREADING    │ │  FRAMEWORK    │ │    TOPICS    │ │ CONCURRENCY  │ │  + REAL WORLD    │
   └────────┬────────┘ └──────┬────────┘ └──────┬────────┘ └──────┬───────┘ └──────┬───────┘ └────────┬─────────┘
            │                 │                  │                  │                │                  │
            │                 │                  │                  │                │                  │
   ┌────────▼────────┐ ┌──────▼────────┐ ┌──────▼────────┐ ┌──────▼───────┐ ┌──────▼───────┐ ┌────────▼─────────┐
   │ • JVM: Stack,   │ │ • Thread      │ │ • Executor    │ │ • ForkJoin   │ │ • @Async     │ │ • Coding Qs      │
   │   Heap, Method  │ │   Runnable    │ │   Service     │ │   Pool       │ │ • @Scheduled │ │ • Design Qs      │
   │   Area          │ │   Callable    │ │ • Thread Pool │ │ • Recursive  │ │ • Task       │ │ • DeadLock Qs    │
   │ • What is a     │ │ • Thread      │ │   Executor    │ │   Task &     │ │   Executor   │ │ • Producer       │
   │   Thread vs     │ │   LifeCycle   │ │ • Callable +  │ │   Action     │ │ • Spring     │ │   Consumer       │
   │   Process       │ │   (6 states)  │ │   Future      │ │ • Parallel   │ │   Tx Mgmt    │ │ • Race Cond.     │
   │ • Race Cond.    │ │ • synchronized│ │ • Completable │ │   Streams    │ │   Isolation  │ │ • Sync Problems  │
   │   concept       │ │   keyword     │ │   Future      │ │ • AQS        │ │ • Spring     │ │ • ThreadLocal    │
   │ • Visibility    │ │ • volatile    │ │ • Atomic Vars │ │   (concept)  │ │   Webflux    │ │   Pitfalls       │
   │   problem       │ │ • wait/notify │ │ • Concurrent  │ │ • LongAdder  │ │   intro      │ │ • Virtual Thread │
   │ • Happens-      │ │   notifyAll   │ │   Collections │ │ • VarHandle  │ │ • R2DBC      │ │   questions      │
   │   Before rule   │ │ • join/sleep  │ │ • Blocking    │ │   concept    │ │   basics     │ │ • System Design  │
   │ • Java Memory   │ │ • interrupt   │ │   Queue       │ │ • StampedLck │ │ • DB Conn    │ │   with           │
   │   Model (JMM)   │ │ • ThreadLocal │ │ • Synchro-    │ │ • ThreadPool │ │   Pool       │ │   Concurrency    │
   │ • CPU Caching   │ │ • Deadlock    │ │   nizers      │ │   Sizing     │ │   HikariCP   │ │ • Mock           │
   │   & Reordering  │ │   (concept)   │ │   ↓           │ │ • Lock Free  │ │ • Cache      │ │   Interviews     │
   │                 │ │ • Liveness    │ │ CountDownLatch│ │   Algorithms │ │   Concurr.   │ │                  │
   │                 │ │   problems    │ │ CyclicBarrier │ │ • Memory     │ │   (Redis)    │ │                  │
   │                 │ │               │ │ Semaphore     │ │   Leaks in   │ │              │ │                  │
   │                 │ │               │ │ Phaser        │ │   Threads    │ │              │ │                  │
   │                 │ │               │ │ Exchanger     │ │ • Java 21    │ │              │ │                  │
   │                 │ │               │ │ • Locks       │ │   Virtual    │ │              │ │                  │
   │                 │ │               │ │   Reentrant   │ │   Threads    │ │              │ │                  │
   │                 │ │               │ │   ReadWrite   │ │              │ │              │ │                  │
   │                 │ │               │ │   Condition   │ │              │ │              │ │                  │
   └─────────────────┘ └───────────────┘ └───────────────┘ └──────────────┘ └──────────────┘ └──────────────────┘
            │                 │                 │                 │                │                  │
   ┌────────▼────────┐ ┌──────▼────────┐ ┌──────▼────────┐ ┌──────▼───────┐ ┌──────▼───────┐ ┌────────▼─────────┐
   │ INTERVIEW WT:   │ │ INTERVIEW WT: │ │ INTERVIEW WT: │ │INTERVIEW WT: │ │INTERVIEW WT: │ │ INTERVIEW WT:    │
   │    ★★☆☆☆     │ │   ★★★★☆     │ │   ★★★★★    │ │   ★★★★☆    │ │   ★★★☆☆   │ │    ★★★★★      │
   └─────────────────┘ └───────────────┘ └───────────────┘ └──────────────┘ └──────────────┘ └──────────────────┘


CONCURRENCY CONCEPTS DEPENDENCY TREE
═════════════════════════════════════

                              ┌──────────────────┐
                              │  VIRTUAL THREADS │  Java 21
                              │  Struct. Conc.   │
                              └────────┬─────────┘
                                       │ builds on
               ┌───────────────────────┼───────────────────────┐
               │                       │                       │
    ┌──────────▼──────────┐ ┌──────────▼──────────┐ ┌─────────▼────────────┐
    │  CompletableFuture  │ │    ForkJoinPool     │ │   Parallel Streams   │
    │  (async pipelines)  │ │ (divide & conquer)  │ │  (data parallelism)  │
    └──────────┬──────────┘ └──────────┬──────────┘ └───────────┬──────────┘
               │                       │                        │
               └───────────────────────┼────────────────────────┘
                                       │ builds on
               ┌───────────────────────┼───────────────────────┐
               │                       │                       │
    ┌──────────▼─────────┐  ┌──────────▼───────────┐ ┌─────────▼──────────┐
    │  ExecutorService   │  │   Atomic Variables   │ │  Concurrent        │
    │  ThreadPoolExec.   │  │   LongAdder / CAS    │ │  Collections       │
    │  ScheduledExec.    │  │   Lock-free          │ │  ConcurrentHashMap │
    └──────────┬─────────┘  └──────────┬───────────┘ └──────────┬─────────┘
               │                       │                        │
               └───────────────────────┼────────────────────────┘
                                       │ builds on
               ┌───────────────────────┼───────────────────────┐
               │                       │                       │
    ┌──────────▼─────────┐  ┌──────────▼───────────┐ ┌─────────▼───────────┐
    │  Callable / Future │  │  ReentrantLock       │ │  Synchronizers      │
    │  (results from     │  │  ReadWriteLock       │ │  CountDownLatch     │
    │   threads)         │  │  StampedLock         │ │  CyclicBarrier      │
    │                    │  │  Condition           │ │  Semaphore, Phaser  │
    └──────────┬─────────┘  └──────────┬───────────┘ └──────────┬──────────┘
               │                       │                        │
               └───────────────────────┼────────────────────────┘
                                       │ builds on
                    ┌──────────────────┼───────────────────┐
                    │                  │                   │
         ┌──────────▼──────┐ ┌─────────▼─────────┐ ┌───────▼─────────────┐
         │  synchronized   │ │    volatile       │ │  wait/notify        │
         │  (intrinsic     │ │  (visibility +    │ │  notifyAll / join   │
         │   lock)         │ │   happens-before) │ │  (coordination)     │
         └──────────┬──────┘ └─────────┬─────────┘ └────────┬────────────┘
                    │                  │                    │
                    └──────────────────┼────────────────────┘
                                       │ builds on
                    ┌──────────────────┼───────────────────┐
                    │                  │                   │
         ┌──────────▼──────┐ ┌─────────▼─────────┐ ┌───────▼────────────┐
         │  Thread         │ │  Java Memory      │ │  JVM Architecture  │
         │  Runnable       │ │  Model (JMM)      │ │  Stack / Heap      │
         │  Thread States  │ │  Happens-Before   │ │  Per-thread Stack  │
         └─────────────────┘ └───────────────────┘ └────────────────────┘
                                    FOUNDATION


WHAT AN INTERVIEWER ACTUALLY TESTS  (by company level)
═══════════════════════════════════════════════════════

  STARTUP / MID-LEVEL SDE              FAANG / SENIOR SDE
  ───────────────────────────          ─────────────────────────────────────
  ✔ Thread lifecycle                   ✔ All of mid-level +
  ✔ synchronized vs volatile           ✔ AQS internals (concept level)
  ✔ Deadlock (detect + fix)            ✔ Lock-free CAS based algorithms
  ✔ ExecutorService + ThreadPool       ✔ Memory model + happens-before
  ✔ Future + CompletableFuture         ✔ ThreadPoolExecutor tuning
  ✔ CountDownLatch / Semaphore         ✔ False sharing / cache lines
  ✔ ConcurrentHashMap internals        ✔ Custom synchronizer via AQS
  ✔ Producer-Consumer problem          ✔ ForkJoin + Parallel Streams
  ✔ @Async in Spring Boot              ✔ Virtual Threads (Java 21)
  ✔ Basic Thread Pool sizing           ✔ System design with concurrency


THREAD POOL SIZING QUICK GUIDE (interview must-know)
═════════════════════════════════════════════════════

  CPU-BOUND tasks:                I/O-BOUND tasks:
  ─────────────────               ─────────────────────────────────────────
  pool size = N cores             pool size = N cores × (1 + wait/compute ratio)
  or N+1 cores                    e.g. 8 cores, 90% I/O wait time:
                                  8 × (1 + 0.9/0.1) = 8 × 10 = ~80 threads
                                  Rule of thumb: 2× to 10× core count


COMMON CONCURRENCY PROBLEMS EVERY INTERVIEWER ASKS
════════════════════════════════════════════════════

  ┌──────────────────┬───────────────────────────┬──────────────────────────────┐
  │     PROBLEM      │       ROOT CAUSE          │          SOLUTION            │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Race Condition   │ Unsynchronized shared     │ synchronized / AtomicInteger │
  │                  │ mutable state             │ / ReentrantLock              │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Deadlock         │ Circular lock dependency  │ Lock ordering / tryLock() /  │
  │                  │                           │ timeout / lock hierarchy     │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Livelock         │ Threads keep responding   │ Randomize retry / backoff    │
  │                  │ to each other, no progress│                              │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Starvation       │ Low-priority threads never│ Fair locks / fair scheduling │
  │                  │ get CPU time              │ new ReentrantLock(true)      │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Visibility Bug   │ CPU cache not flushed to  │ volatile / synchronized /    │
  │                  │ main memory               │ AtomicReference              │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Missed Signal    │ notify() before wait()    │ Always check condition in    │
  │                  │                           │ while loop                   │
  ├──────────────────┼───────────────────────────┼──────────────────────────────┤
  │ Memory Leak      │ ThreadLocal not removed   │ Always threadLocal.remove()  │
  │                  │ in thread pool threads    │ in finally block             │
  └──────────────────┴───────────────────────────┴──────────────────────────────┘
```

---
## Detailed Roadmap — Week by Week

---
### ⚡ Level 0 — Prerequisites & JVM Basics (Week 1)

**Why this first:**
Every concurrency bug, every keyword, every tool — all exist because of how the JVM and CPU hardware work. Without this, you'll memorize syntax but never truly understand WHY things break.

**Topics:**
```
1. What is a Process vs Thread
   - Process = isolated memory space
   - Thread = lightweight, shares heap with other threads in same process
   - JVM = one process, multiple threads sharing the heap

2. JVM Memory Areas relevant to concurrency
   - Heap          → shared by ALL threads (where objects live)
   - Thread Stack  → private to EACH thread (local vars, method calls)
   - CPU Registers → private to each CPU core (fastest cache)
   - CPU L1/L2/L3 Cache → between register and main memory

3. The Visibility Problem (WHY volatile exists)
   - Thread A writes x=1 to its CPU cache
   - Thread B reads x — might still see x=0 (CPU cache not flushed yet)
   - volatile forces write to main memory and read from main memory

4. The Reordering Problem (WHY happens-before exists)
   - JVM and CPU are free to reorder instructions for performance
   - Thread A: a=1; flag=true; might become flag=true; a=1; (reordered)
   - Thread B sees flag=true but reads a=0 → incorrect behavior

5. Java Memory Model (JMM)
   - The contract between JVM and developer
   - Defines when writes by one thread become visible to others
   - The Happens-Before relationship:
     unlock happens-before lock on same monitor
     write to volatile happens-before read of same volatile
     thread.start() happens-before any code in that thread
     thread finishes happens-before thread.join() returns

6. Race Condition (concept)
   - Two threads read-modify-write same variable without synchronization
   - counter++ is NOT atomic (it is: read counter, add 1, write counter)
   - 3 separate operations → thread can be interrupted between any two
```

**Practice:**
- Draw the JVM memory layout and trace what happens with two threads modifying a shared counter
- Write a simple race condition bug, see it manifest, then fix it three ways (synchronized, volatile, AtomicInteger)
---
### 🧱 Level 1 — Foundation Threading (Week 2-3)

**Topics with interview weight:**

```
1. Thread and Runnable               ★★★★★
   - Thread extends / Runnable implements
   - Why Runnable is preferred over extending Thread
   - Thread.currentThread(), Thread.getName(), Thread.setName()

2. Thread Lifecycle — 6 States       ★★★★★
   NEW → RUNNABLE → BLOCKED → WAITING → TIMED_WAITING → TERMINATED
   Know every transition:
   - start()              → NEW       to RUNNABLE
   - synchronized blocked → RUNNABLE  to BLOCKED
   - wait()               → RUNNABLE  to WAITING
   - wait(timeout)        → RUNNABLE  to TIMED_WAITING
   - sleep()              → RUNNABLE  to TIMED_WAITING
   - notify()             → WAITING   to BLOCKED (waiting for lock)
   - join()               → calling thread to WAITING

3. synchronized keyword              ★★★★★
   - Instance method     → locks on 'this' object
   - Static method       → locks on Class object
   - Synchronized block  → locks on specified object
   - Reentrant (same thread can re-enter its own lock)

4. volatile keyword                  ★★★★☆
   - Guarantees visibility (no CPU cache)
   - Guarantees happens-before for reads/writes
   - Does NOT guarantee atomicity (i++ is still not safe with volatile)
   - Use for: flags, state variables read by many / written by one

5. wait() / notify() / notifyAll()   ★★★★★
   - (covered in depth in previous story)
   - Always in synchronized, always while loop

6. join() / sleep() / interrupt()    ★★★★☆
   - join()      → wait for thread to die
   - sleep()     → pause current thread (does NOT release lock)
   - interrupt() → set interrupt flag (does not kill thread immediately)
   - isInterrupted() vs interrupted() (instance vs static, clears flag or not)

7. ThreadLocal<T>                    ★★★★☆
   - Per-thread storage — each thread has its own copy
   - Common use: storing user session, DB connection, security context
   - DANGER: in thread pools, always call remove() in finally block
     (threads are reused — stale ThreadLocal data leaks across requests)

8. Deadlock                          ★★★★★
   - Four conditions (Coffman conditions):
     1. Mutual Exclusion
     2. Hold and Wait
     3. No Preemption
     4. Circular Wait
   - How to detect: thread dump (jstack), deadlock detection in JConsole
   - How to prevent:
     Always acquire locks in consistent order
     Use tryLock(timeout) from ReentrantLock
     Use lock hierarchy / lock leveling

9. Liveness problems                 ★★★☆☆
   - Livelock: threads respond to each other but make no progress
   - Starvation: thread never gets CPU time (fix: fair locks)
```

**Practice problems:**
- Implement a thread-safe singleton (double-checked locking — and explain why volatile is needed)
- Implement a simple producer-consumer with wait/notify
- Write a program that deliberately deadlocks, then fix it
---
### ⚙️ Level 2 — java.util.concurrent Framework (Week 4-5)

**This is the highest-interview-weight section. Most interview questions come from here.**

```
1. ExecutorService & ThreadPoolExecutor              ★★★★★
   - Executors.newFixedThreadPool(n)
   - Executors.newCachedThreadPool()
   - Executors.newSingleThreadExecutor()
   - Why you should prefer explicit ThreadPoolExecutor over factory methods
   - Know all 7 constructor parameters of ThreadPoolExecutor:
     corePoolSize, maximumPoolSize, keepAliveTime, unit,
     workQueue, threadFactory, rejectedExecutionHandler
   - Know all RejectedExecutionHandler policies:
     AbortPolicy (default — throws RejectedExecutionException)
     CallerRunsPolicy (caller thread runs the task — natural backpressure)
     DiscardPolicy (silently drops)
     DiscardOldestPolicy (drops oldest waiting task)
   - Lifecycle: submit → shutdown → awaitTermination

2. Callable and Future                               ★★★★★
   - Callable vs Runnable (returns value, throws checked exception)
   - Future.get() — blocking
   - Future.get(timeout, unit) — bounded blocking
   - Future.cancel(mayInterruptIfRunning)
   - Future.isDone() / isCancelled()
   - ExecutionException wraps exception thrown inside Callable

3. CompletableFuture                                 ★★★★★
   - supplyAsync(Supplier, executor)
   - thenApply(Function)    → transform result (sync)
   - thenCompose(Function)  → chain another async step (flatMap)
   - thenCombine(CF, BiFunction) → combine two independent CFs
   - thenAccept(Consumer)   → consume result, return Void
   - thenRun(Runnable)      → run after, no input/output
   - exceptionally(Function) → handle errors in chain
   - handle(BiFunction)     → handle both result and error
   - allOf(CF...)           → wait for all
   - anyOf(CF...)           → take first to complete
   - join() vs get()        → join() throws unchecked, get() throws checked

4. Atomic Variables                                  ★★★★☆
   - AtomicInteger, AtomicLong, AtomicBoolean, AtomicReference
   - compareAndSet(expected, update) — CAS operation
   - getAndIncrement() / incrementAndGet()
   - LongAdder — better than AtomicLong under high contention
     (uses striping — multiple cells, reduced contention)
   - When to use: simple counters, flags, references — no lock needed

5. Concurrent Collections                            ★★★★★
   - ConcurrentHashMap:
     Java 7: segment-level locking (16 segments by default)
     Java 8+: CAS + node-level locking (much finer granularity)
     putIfAbsent, computeIfAbsent, merge — atomic compound ops
     size() is approximate (not locked)
   - CopyOnWriteArrayList / CopyOnWriteArraySet:
     Every write creates a new copy of underlying array
     Reads are lock-free (snapshot)
     Best for: few writes, many reads, small collections
   - LinkedBlockingQueue / ArrayBlockingQueue:
     Bounded vs unbounded queue
     put() blocks when full, take() blocks when empty
   - ConcurrentLinkedQueue: non-blocking, lock-free queue (CAS-based)
   - Collections.synchronizedList() vs ConcurrentHashMap:
     synchronized wrappers lock entire collection per method
     compound operations (check-then-act) still need external sync
     ConcurrentHashMap provides atomic compound ops natively

6. ReentrantLock and Condition                       ★★★★★
   - Advantages over synchronized:
     tryLock() — non-blocking attempt
     tryLock(timeout, unit) — bounded wait
     lockInterruptibly() — respond to interrupt
     Fairness mode: new ReentrantLock(true)
     Multiple Conditions per lock
   - Always unlock in finally block
   - Condition.await() vs wait()
     await() → must hold ReentrantLock (not synchronized)
     More than one condition per lock is the main advantage

7. ReadWriteLock / ReentrantReadWriteLock            ★★★☆☆
   - Multiple readers OR one writer (never both)
   - readLock() — shared (multiple can hold simultaneously)
   - writeLock() — exclusive (one at a time, blocks all readers)
   - When to use: data mostly read, occasionally updated
     (e.g. configuration map, caches)

8. StampedLock (Java 8)                              ★★★☆☆
   - Adds optimistic reading on top of read/write
   - tryOptimisticRead() — get a stamp without locking
   - validate(stamp) — check if any write happened since stamp
   - If validate fails → fall back to full readLock
   - Best throughput for read-heavy scenarios

9. Synchronizers                                     ★★★★★
   CountDownLatch:
   - One-time countdown (cannot be reset)
   - Use: wait for N services/threads to start or finish
   - await() — wait for count to reach 0
   - countDown() — decrement count

   CyclicBarrier:
   - All N parties wait at barrier before any proceeds
   - Can be reset and reused (unlike CountDownLatch)
   - Use: parallel phases of computation, data processing stages
   - await() — arrive and wait for others
   - Optional Runnable triggered when barrier trips

   Semaphore:
   - N permits — limits concurrent access
   - acquire() — take a permit (block if none)
   - release() — return a permit
   - Use: connection pools, rate limiting, bounded resources

   Phaser (Java 7):
   - Flexible barrier — dynamic party registration
   - Combines CountDownLatch + CyclicBarrier features
   - register()/deregister() — add/remove parties dynamically
   - Use: complex multi-phase tasks with variable participants

   Exchanger:
   - Two threads swap objects at a rendezvous point
   - Use: pipeline stages, genetic algorithms, double-buffering
   - exchange(V) — give your object, receive theirs

10. BlockingQueue Implementations                    ★★★★☆
    ArrayBlockingQueue  — bounded, backed by array, single lock
    LinkedBlockingQueue — optionally bounded, two locks (head/tail)
    SynchronousQueue    — zero capacity, direct handoff only
    PriorityBlockingQueue — unbounded, natural/custom ordering
    DelayQueue          — elements available after delay expires
    LinkedTransferQueue — like SynchronousQueue but with queue backup
```

**Practice problems:**
- Build a thread-safe bounded cache using ReentrantReadWriteLock
- Build a connection pool using Semaphore
- Implement producer-consumer using BlockingQueue (should be trivial with BlockingQueue)
- Write a CompletableFuture pipeline: fetch user → fetch orders → compute total → save to DB (async chain)
- Build ThreadPoolExecutor with bounded queue and CallerRunsPolicy, observe backpressure
---
### 🔬 Level 3 — Advanced Topics (Week 6-7)

```
1. ForkJoinPool and RecursiveTask/RecursiveAction     ★★★★☆
   - Work-stealing: idle threads steal tasks from busy threads' queues
   - Best for: recursive divide-and-conquer (array sum, merge sort, tree traversal)
   - RecursiveTask<V>  → compute() returns a value
   - RecursiveAction   → compute() returns void
   - fork() + join() vs invokeAll()
   - ForkJoinPool.commonPool() — shared default pool (also used by parallel streams)
   - Danger: long blocking tasks in commonPool block parallel streams

2. Parallel Streams                                   ★★★★☆
   - parallelStream() delegates to ForkJoinPool.commonPool()
   - When to use: CPU-bound, large data, stateless operations
   - When NOT to use: small data, I/O operations, shared mutable state
   - Use custom ForkJoinPool to isolate parallel stream from commonPool:
     ForkJoinPool myPool = new ForkJoinPool(4);
     myPool.submit(() -> list.parallelStream().map(...).collect(...)).get();

3. AbstractQueuedSynchronizer (AQS)                   ★★★☆☆
   - The backbone implementation of most locks and synchronizers
   - ReentrantLock, Semaphore, CountDownLatch all built on AQS
   - Core idea: integer state + FIFO queue of waiting threads
   - Exclusive mode (ReentrantLock) vs Shared mode (Semaphore, CountDownLatch)
   - For interview: understand the concept, not the implementation details
   - Know: tryAcquire / tryRelease / acquire / release

4. LongAdder and LongAccumulator                      ★★★☆☆
   - High-throughput alternatives to AtomicLong
   - Maintains multiple cells (one per contending thread) + base
   - sum() aggregates them — may be stale
   - Use when: high contention increments (metrics, counters)
   - AtomicLong better when: you need precise consistent reads

5. ThreadLocal advanced patterns                      ★★★★☆
   - InheritableThreadLocal — child thread inherits parent's value
   - ThreadLocal in Spring: SecurityContextHolder, TransactionSynchronizationManager
   - Memory leak pattern in thread pools:
     Thread is reused → ThreadLocal value persists across requests
     FIX: always threadLocal.remove() in finally or use request-scope beans

6. Thread Pool Sizing (the formula)                   ★★★★★
   CPU-bound:    threads = N (or N+1 to handle one page fault)
   I/O-bound:    threads = N × (1 + W/C)  where W=wait time, C=compute time
   Mixed:        profile first, then tune

7. False Sharing (cache line problem)                 ★★★☆☆
   - CPU cache lines are 64 bytes
   - If two threads write to different variables on the SAME cache line
     → each write invalidates the other's cache → massive contention
   - Fix: padding (add dummy fields to push variables to different cache lines)
   - Java 8+: @Contended annotation (in jdk.internal.vm.annotation)
   - LongAdder uses this internally (why it beats AtomicLong at scale)

8. Java 21 Virtual Threads                            ★★★★★
   - Thread.ofVirtual().start(runnable)
   - Executors.newVirtualThreadPerTaskExecutor()
   - Cheap: millions of virtual threads vs thousands of platform threads
   - Blocking is fine: JVM parks virtual thread, not the platform thread
   - Do NOT pool virtual threads (defeats the purpose — they are cheap)
   - Pinning problem: synchronized blocks pin virtual thread to carrier thread
     → prefer ReentrantLock over synchronized in virtual thread code
   - Structured Concurrency (preview): StructuredTaskScope — scope-based
     lifetime management for forked virtual threads

9. Memory Leaks in Concurrency                        ★★★★☆
   - ThreadLocal not removed in thread pool
   - Listeners/callbacks registered but never deregistered
   - Tasks submitted to executor but executor never shut down
   - ConcurrentHashMap holding strong references to objects
   - Threads holding references to large objects preventing GC
```
---
### 🍃 Level 4 — Spring Boot Concurrency (Week 7-8)

```
1. @Async                                             ★★★★★
   - Add @EnableAsync to config class
   - Annotate method with @Async → runs in separate thread
   - Default: SimpleAsyncTaskExecutor (creates new thread per call — dangerous!)
   - Always configure a ThreadPoolTaskExecutor bean
   - @Async methods MUST be in a different bean (Spring proxy limitation)
   - @Async on private or same-class methods = silently ignored
   - Return: void, Future<T>, CompletableFuture<T>, ListenableFuture<T>

   Example configuration:
   @Bean("taskExecutor")
   public Executor taskExecutor() {
       ThreadPoolTaskExecutor exec = new ThreadPoolTaskExecutor();
       exec.setCorePoolSize(5);
       exec.setMaxPoolSize(20);
       exec.setQueueCapacity(100);
       exec.setThreadNamePrefix("async-");
       exec.setRejectedExecutionHandler(new CallerRunsPolicy());
       exec.initialize();
       return exec;
   }

2. @Scheduled                                         ★★★☆☆
   - @EnableScheduling in config
   - fixedRate — run every N ms regardless of completion
   - fixedDelay — wait N ms AFTER last completion then run again
   - cron — standard cron expression
   - Default: single-threaded scheduler — tasks queue up if one is slow
   - Fix: configure ScheduledExecutorService or TaskScheduler bean

3. Spring Transaction Isolation Levels                ★★★★★
   (concurrency at database level — always asked in Spring interviews)
   - READ_UNCOMMITTED: dirty reads possible (fastest, least safe)
   - READ_COMMITTED:   no dirty reads, phantom/non-repeatable reads possible
   - REPEATABLE_READ:  no dirty/non-repeatable reads, phantom reads possible
   - SERIALIZABLE:     fully isolated, slowest

   - Default in Spring: follows database default (usually READ_COMMITTED)
   - Concurrency problems:
     Dirty Read:           read uncommitted data from another transaction
     Non-Repeatable Read:  same query returns different rows in same transaction
     Phantom Read:         new rows appear between two reads in same transaction

4. Spring's TaskExecutor / ThreadPoolTaskExecutor     ★★★★☆
   - Spring's wrapper around java.util.concurrent.ThreadPoolExecutor
   - ThreadPoolTaskExecutor: most important, wraps ThreadPoolExecutor
   - ConcurrentTaskExecutor: wraps any java.util.concurrent.Executor
   - SimpleAsyncTaskExecutor: creates new thread per task (only for testing)

5. HikariCP Connection Pool                           ★★★★☆
   - Default connection pool in Spring Boot 2+
   - maximumPoolSize: default 10 (tune based on DB + application threads)
   - connectionTimeout: how long to wait for connection from pool
   - idleTimeout: how long idle connections are kept
   - maximumPoolSize of HikariCP should roughly match ThreadPoolExecutor size
   - Too few DB connections = thread pool threads blocked waiting for connection
   - Too many DB connections = DB overwhelmed

6. Spring WebFlux / Reactive                          ★★★☆☆
   - Project Reactor (Mono / Flux)
   - Non-blocking I/O on Netty (event loop, not thread-per-request)
   - flatMap = async composition, map = sync transformation
   - subscribeOn → which thread subscribes (starts execution)
   - publishOn → which thread publishes (downstream operators)
   - Schedulers.boundedElastic() — for blocking code in reactive pipeline
   - Do NOT use in same app as Spring MVC without careful threading config

7. Cache Concurrency (Redis / Caffeine)               ★★★☆☆
   - Cache-aside pattern thread safety
   - Double-checked locking for cache population
   - Redis SETNX / Lua scripts for distributed locking
   - @Cacheable thread safety in Spring (uses ConcurrentMap internally)
   - Caffeine: compute() is atomic — safe for cache population
```
---
### 🏆 Level 5 — Interview Ready (Week 9-12)

```
CLASSIC CODING PROBLEMS every Java concurrency interview covers:

1. Thread-safe Singleton (Double-Checked Locking)
   - Why volatile is required on the instance field
   - What breaks without it (partial initialization visible to other threads)

2. Producer-Consumer
   - With wait/notify
   - With BlockingQueue (trivially easy, show you know both)

3. Print numbers alternately from two threads
   - Thread 1 prints odd, Thread 2 prints even, in order 1 2 3 4...
   - Uses wait/notify, shows monitor understanding

4. Dining Philosophers
   - Classic deadlock scenario
   - Solutions: lock ordering, tryLock with timeout, one left-handed philosopher

5. Read-Write problem
   - Multiple readers, one writer
   - Implement with ReadWriteLock
   - Or with wait/notify from scratch (tests deeper understanding)

6. Implement a thread-safe blocking queue from scratch
   - Using ReentrantLock + 2 Conditions (notFull, notEmpty)
   - Very popular senior interview question

7. Rate Limiter
   - Using Semaphore
   - Fixed window vs sliding window

8. Connection Pool
   - Using Semaphore + ConcurrentLinkedQueue

9. Future/CompletableFuture chains
   - Chain 3 async service calls
   - Handle errors with exceptionally
   - Run two in parallel, combine with thenCombine

10. Deadlock detection in a thread dump
    - Given a jstack output, identify deadlock
    - Which thread holds which lock


SYSTEM DESIGN WITH CONCURRENCY (senior SDE level):

- Design a thread-safe LRU Cache (LinkedHashMap + ReentrantReadWriteLock)
- Design a rate limiter service (Semaphore / token bucket)
- Design an async notification system (@Async + queue + retry)
- Design a task scheduler (ScheduledExecutorService / Quartz)
- Design a parallel bulk data processor (ForkJoinPool / parallel streams)
- Explain how to handle concurrency in a microservices environment
  (optimistic locking, distributed locks via Redis, event sourcing)
```

---
## Study Resources (Curated)

```
BOOKS (in order of reading):
  1. Java Concurrency in Practice — Brian Goetz et al.
     (THE bible — read chapters 1-5 and 12-16 at minimum)
  2. The Art of Multiprocessor Programming — Herlihy & Shavit
     (for senior/FAANG — lock-free algorithms, AQS depth)

PRACTICE:
  1. Write every synchronizer from scratch (CountDownLatch, Semaphore,
     BlockingQueue) using only ReentrantLock + Condition
  2. LeetCode: "Print in Order", "Print FooBar Alternately",
     "Print Zero Even Odd", "Building H2O", "The Dining Philosophers"
  3. Read JDK source: ReentrantLock, CountDownLatch, ConcurrentHashMap
     (critical parts are ~50 lines — very readable)

TOOLS:
  jstack   — take thread dump, spot deadlocks
  jconsole — GUI thread monitoring, deadlock detection
  VisualVM — thread profiling, CPU usage per thread
  IntelliJ Thread Concurrency plugin — annotates @GuardedBy etc.
```

---
## The Priority Matrix (what to study first if time is short)

```
╔═══════════════════════════════════════════════════════════════════╗
║  ABSOLUTE MUST (every interview, every company)                   ║
║  ─────────────────────────────────────────────────────────────    ║
║  Thread lifecycle · synchronized · volatile · wait/notify/join    ║
║  ExecutorService · ThreadPoolExecutor · Future · Callable         ║
║  CompletableFuture basics · ConcurrentHashMap · AtomicInteger     ║
║  CountDownLatch · Semaphore · ReentrantLock · Deadlock            ║
╠═══════════════════════════════════════════════════════════════════╣
║  HIGHLY LIKELY (mid-senior interviews)                            ║
║  ─────────────────────────────────────────────────────────────    ║
║  ThreadLocal · BlockingQueue · ReadWriteLock · CyclicBarrier      ║
║  ForkJoinPool · Parallel Streams · Spring @Async · LongAdder      ║
║  Thread pool sizing · Transaction isolation · HikariCP            ║
╠═══════════════════════════════════════════════════════════════════╣
║  GOOD TO KNOW (senior / FAANG)                                    ║
║  ─────────────────────────────────────────────────────────────    ║
║  AQS internals · StampedLock · Phaser · Exchanger                 ║
║  False sharing · VarHandle · Virtual Threads (Java 21)            ║
║  Structured Concurrency · Reactive (WebFlux) · Lock-free algos   ║
╚═══════════════════════════════════════════════════════════════════╝
```

The entire journey can be summarized in one sentence:

> **Start from WHY things break (JMM, visibility, atomicity) → learn the primitives (Thread, synchronized, wait/notify) → master the framework (ExecutorService, CompletableFuture, collections) → apply in Spring Boot → practice classic interview problems — and you will be ready.**


-------

-----

# Java Concurrency Mastery Roadmap — All Topics by Week

---

## WEEK 1 — Level 0 — Prerequisites & JVM Basics

```css
1.  JVM Architecture Overview
2.  JVM Stack — what it is, stack frames, local variables
3.  JVM Heap — what it is, Young/Old generation, GC basics
4.  JVM Method Area (Metaspace) — class data, static variables, bytecode
5.  What is a Process
6.  What is a Thread
7.  Process vs Thread — differences, isolation, communication
8.  Race Condition — concept, what it is, why it happens
9.  The Visibility Problem — why CPU caches cause bugs
10. CPU Caching and Reordering — L1/L2/L3 cache, store buffers
11. The Reordering Problem — compiler and CPU reorder instructions
12. Java Memory Model (JMM) — the formal contract
13. Happens-Before Rule — what it is, why it matters
14. The 8 Happens-Before rules in JMM
```

---

## WEEK 2-3 — Level 1 — Foundation Threading

```css
15. Thread class — creating, starting, naming threads
16. Runnable interface — task vs thread separation
17. Callable interface — tasks that return values
18. Thread Lifecycle — all 6 states
        NEW → RUNNABLE → BLOCKED → WAITING → TIMED_WAITING → TERMINATED
19. Thread state transitions — what causes each transition
20. synchronized keyword — instance method, static method, block
21. Intrinsic Lock (Monitor) — how synchronized works internally
22. volatile keyword — visibility guarantee, happens-before
23. volatile — what it does NOT guarantee (atomicity)
24. wait() — how it works, releasing lock, waiting
25. notify() — waking one thread from wait set
26. notifyAll() — waking all threads from wait set
27. wait/notify golden rule — always while, never if
28. Spurious wakeups — what they are, how to handle
29. join() — waiting for a thread to die
30. join() with timeout — join(millis)
31. sleep() — pausing current thread, does NOT release lock
32. sleep() vs wait() — key differences
33. interrupt() — setting the interrupt flag
34. isInterrupted() vs interrupted() — instance vs static, flag clearing
35. InterruptedException — when thrown, how to handle
36. ThreadLocal<T> — per-thread storage
37. ThreadLocal — memory leak in thread pools, always remove()
38. InheritableThreadLocal — child thread inherits parent value
39. Deadlock — what it is, the four Coffman conditions
40. Deadlock — how to detect (jstack, jconsole)
41. Deadlock — how to prevent (lock ordering, tryLock)
42. Livelock — threads respond to each other, no progress
43. Starvation — thread never gets CPU time
44. Daemon vs Non-Daemon threads — JVM exit behavior
45. Thread Priority — hint to scheduler, not a guarantee
```

---

## WEEK 4-5 — Level 2 — java.util.concurrent Framework

```css
46. Executor interface — execute(Runnable)
47. ExecutorService interface — lifecycle, submit, shutdown
48. Executors factory class — newFixedThreadPool, newCachedThreadPool,
        newSingleThreadExecutor, newScheduledThreadPool
49. ThreadPoolExecutor — all 7 constructor parameters
        corePoolSize, maximumPoolSize, keepAliveTime,
        unit, workQueue, threadFactory, rejectedExecutionHandler
50. ThreadPoolExecutor — execution flow
        (when new thread created vs queued vs rejected)
51. ThreadPoolExecutor — lifecycle methods
        shutdown(), shutdownNow(), awaitTermination()
52. RejectedExecutionHandler — all 4 policies
        AbortPolicy, CallerRunsPolicy,
        DiscardPolicy, DiscardOldestPolicy
53. ThreadFactory — naming threads, setting daemon, uncaught exception handler
54. Callable<V> — vs Runnable, returns value, throws checked exception
55. Future<V> — get(), cancel(), isDone(), isCancelled()
56. Future.get() — blocking, timeout variant
57. ExecutionException — wraps exception from Callable
58. CompletableFuture — supplyAsync(), runAsync()
59. CompletableFuture — thenApply() — sync transformation
60. CompletableFuture — thenCompose() — async chaining (flatMap)
61. CompletableFuture — thenCombine() — combine two independent futures
62. CompletableFuture — thenAccept(), thenRun()
63. CompletableFuture — exceptionally(), handle()
64. CompletableFuture — allOf(), anyOf()
65. CompletableFuture — join() vs get()
66. CompletionStage interface — the parent of CompletableFuture
67. AtomicInteger — get, set, incrementAndGet, compareAndSet
68. AtomicLong — same operations as AtomicInteger
69. AtomicBoolean — atomic flag operations
70. AtomicReference<V> — atomic object reference operations
71. CAS — Compare-And-Swap — how atomics work at CPU level
72. LongAdder — high throughput counter, striped cells
73. LongAccumulator — generalized LongAdder
74. LongAdder vs AtomicLong — when to use which
75. ConcurrentHashMap — thread-safe map, no full-table lock
76. ConcurrentHashMap — Java 7 (segments) vs Java 8+ (CAS + node lock)
77. ConcurrentHashMap — putIfAbsent, computeIfAbsent, merge
78. CopyOnWriteArrayList — snapshot on write, lock-free reads
79. CopyOnWriteArraySet — same model as CopyOnWriteArrayList
80. ConcurrentLinkedQueue — non-blocking, lock-free queue
81. Collections.synchronizedList() — vs ConcurrentHashMap trade-offs
82. BlockingQueue interface — put(), take(), offer(), poll()
83. ArrayBlockingQueue — bounded, backed by array, single lock
84. LinkedBlockingQueue — optionally bounded, two locks (head/tail)
85. SynchronousQueue — zero capacity, direct handoff only
86. PriorityBlockingQueue — unbounded, priority ordering
87. DelayQueue — elements available after delay
88. LinkedTransferQueue — transfer(), tryTransfer()
89. Lock interface — lock(), unlock(), tryLock(), lockInterruptibly()
90. ReentrantLock — reentrant mutual exclusion
91. ReentrantLock — tryLock() with timeout — avoids deadlock
92. ReentrantLock — fairness mode — new ReentrantLock(true)
93. ReentrantLock — always unlock in finally block
94. Condition — await(), signal(), signalAll()
95. Condition — vs wait/notify — multiple conditions per lock
96. ReadWriteLock interface — readLock(), writeLock()
97. ReentrantReadWriteLock — shared reads, exclusive writes
98. StampedLock — optimistic read, tryOptimisticRead(), validate()
99. CountDownLatch — await(), countDown(), one-time countdown
100. CyclicBarrier — await(), reset(), reusable barrier
101. CountDownLatch vs CyclicBarrier — key differences
102. Semaphore — acquire(), release(), permit-based access control
103. Phaser — register(), arrive(), arriveAndAwaitAdvance()
        dynamic parties, multi-phase
104. Exchanger<V> — exchange() — two-thread object swap
105. LockSupport — park(), unpark() — low-level primitives
```

---
## WEEK 6-7 — Level 3 — Advanced Topics

```css
106. ForkJoinPool — work-stealing algorithm
107. ForkJoinPool — commonPool() — shared default pool
108. RecursiveTask<V> — compute() returns value
109. RecursiveAction — compute() returns void
110. fork() + join() — submitting and collecting subtasks
111. ForkJoinPool — when to use vs ThreadPoolExecutor
112. Parallel Streams — parallelStream(), parallel()
113. Parallel Streams — uses ForkJoinPool.commonPool() internally
114. Parallel Streams — when to use, when NOT to use
115. Parallel Streams — custom ForkJoinPool to isolate from commonPool
116. AbstractQueuedSynchronizer (AQS) — the backbone of locks
117. AQS — integer state + FIFO wait queue concept
118. AQS — exclusive mode (ReentrantLock) vs shared mode (Semaphore)
119. AQS — tryAcquire, tryRelease, acquire, release — concept
120. False Sharing — cache line problem (64 bytes)
121. False Sharing — @Contended annotation, padding fix
122. LongAdder internals — Striped64, cell-based striping
123. ThreadLocal advanced — Spring's SecurityContextHolder,
        TransactionSynchronizationManager
124. ThreadLocal memory leak — thread pool reuse problem
        always call remove() in finally
125. Thread Pool Sizing — CPU-bound formula: N cores or N+1
126. Thread Pool Sizing — I/O-bound formula: N × (1 + W/C)
127. Memory Leaks in concurrency — ThreadLocal, listeners,
        executor not shut down, strong references
128. VarHandle — getVolatile, setVolatile, compareAndSet
        (conceptual — replacement for Unsafe)
129. Java 21 Virtual Threads — Thread.ofVirtual()
130. Virtual Threads — Executors.newVirtualThreadPerTaskExecutor()
131. Virtual Threads — platform thread vs virtual thread differences
132. Virtual Threads — pinning problem with synchronized blocks
133. Virtual Threads — do NOT pool virtual threads
134. Structured Concurrency — StructuredTaskScope (Java 21 preview)
135. Structured Concurrency — ShutdownOnFailure, ShutdownOnSuccess
136. Lock-free algorithms — concept, CAS loops, ABA problem
```

---
## WEEK 7-8 — Level 4 — Spring Boot Concurrency

```css
137. @EnableAsync — enabling async support in Spring
138. @Async — running methods in separate thread
139. @Async — default SimpleAsyncTaskExecutor danger
        (creates new thread per call)
140. @Async — configuring ThreadPoolTaskExecutor bean
141. @Async — return types: void, Future, CompletableFuture
142. @Async — same-class call limitation (Spring proxy issue)
143. @Async — private method limitation
144. @EnableScheduling — enabling scheduling support
145. @Scheduled — fixedRate, fixedDelay, cron expressions
146. @Scheduled — default single-threaded scheduler danger
147. @Scheduled — configuring TaskScheduler bean
148. ThreadPoolTaskExecutor — Spring wrapper around ThreadPoolExecutor
149. Spring TaskExecutor hierarchy —
        ThreadPoolTaskExecutor, ConcurrentTaskExecutor,
        SimpleAsyncTaskExecutor
150. Transaction Isolation Levels —
        READ_UNCOMMITTED, READ_COMMITTED,
        REPEATABLE_READ, SERIALIZABLE
151. Transaction concurrency problems —
        Dirty Read, Non-Repeatable Read, Phantom Read
152. Spring default isolation — follows database default
153. Optimistic vs Pessimistic Locking in Spring Data JPA
154. @Lock annotation — LockModeType in Spring Data
155. HikariCP — default connection pool in Spring Boot 2+
156. HikariCP — maximumPoolSize, connectionTimeout,
        idleTimeout tuning
157. HikariCP pool size — matching with ThreadPoolExecutor size
158. Spring WebFlux — Project Reactor, Mono, Flux
159. WebFlux — non-blocking I/O on Netty event loop
160. WebFlux — flatMap vs map (async vs sync)
161. WebFlux — subscribeOn vs publishOn
162. WebFlux — Schedulers.boundedElastic() for blocking code
163. R2DBC — reactive database connectivity basics
164. Cache concurrency — @Cacheable thread safety
165. Redis distributed locking — SETNX, Lua scripts
166. Caffeine cache — compute() atomicity
```

---
## WEEK 9-12 — Level 5 — Interview Ready + Real World

### Classic Coding Problems
```css
167. Thread-safe Singleton — Double-Checked Locking
        why volatile is required on instance field
168. Thread-safe Singleton — Initialization-on-demand Holder idiom
169. Producer-Consumer — using wait() / notify()
170. Producer-Consumer — using BlockingQueue (trivial version)
171. Print numbers alternately from two threads
        (Thread 1: odd, Thread 2: even — in order 1 2 3 4...)
172. Dining Philosophers — classic deadlock scenario + solutions
173. Read-Write problem — with ReentrantReadWriteLock
174. Read-Write problem — with wait/notify from scratch
175. Implement BlockingQueue from scratch
        using ReentrantLock + two Conditions (notFull, notEmpty)
176. Rate Limiter — using Semaphore, fixed window
177. Rate Limiter — sliding window variant
178. Connection Pool — using Semaphore + ConcurrentLinkedQueue
179. CompletableFuture chain — 3 async service calls,
        error handling, parallel execution, combine results
180. Thread-safe LRU Cache —
        LinkedHashMap + ReentrantReadWriteLock
181. Deadlock detection from thread dump (jstack output)
182. Print FooBar alternately — LeetCode style
183. Print Zero Even Odd — LeetCode style
184. Building H2O — LeetCode style (like HHO problem)
185. The Dining Philosophers — LeetCode style
```

### System Design with Concurrency
```css
186. Design a thread-safe LRU Cache
187. Design a Rate Limiter service (token bucket algorithm)
188. Design an async notification system
        (@Async + queue + retry + dead letter)
189. Design a Task Scheduler
        (ScheduledExecutorService / Quartz)
190. Design a parallel bulk data processor
        (ForkJoinPool / parallel streams)
191. Design a connection pool from scratch
192. Concurrency in microservices —
        optimistic locking, distributed locks via Redis,
        event sourcing, saga pattern
```

### Interview Question Categories
```css
193. Thread lifecycle questions — state transitions
194. synchronized vs volatile — when to use which
195. synchronized vs ReentrantLock — trade-offs
196. Deadlock questions — detect, prevent, avoid
197. ExecutorService questions — pool types, sizing, rejection
198. Future vs CompletableFuture — differences
199. ConcurrentHashMap internals — Java 7 vs Java 8+
200. AtomicInteger vs synchronized vs volatile — which when
201. ThreadLocal questions — use cases, pitfalls, memory leaks
202. Virtual Thread questions — Java 21, when to use
203. wait() vs sleep() — differences
204. wait() vs join() — differences
205. notify() vs notifyAll() — when to use which
206. AQS concept questions — how locks are built on it
207. CountDownLatch vs CyclicBarrier vs Phaser — differences
208. Parallel Streams — pitfalls, ForkJoinPool interaction
```

### Tools and Operational Topics
```css
209. jstack — taking thread dumps, reading output
210. jconsole — GUI thread monitoring, deadlock detection
211. VisualVM — thread profiling, CPU usage per thread
212. Thread naming best practices —
        always name threads for debugging
213. UncaughtExceptionHandler — handling thread exceptions
214. Graceful shutdown — shutdown() + awaitTermination() pattern
215. Thread pool monitoring —
        getActiveCount(), getQueueSize(),
        getCompletedTaskCount()
```

---
## Quick Reference — Topic Count Per Week

```cs
╔══════════════════════╦═════════════════════════════╦══════════╗
║  Week                ║  Level / Focus              ║  Topics  ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 1              ║  Level 0 — JVM Basics       ║   1-14   ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 2-3            ║  Level 1 — Foundation       ║  15-45   ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 4-5            ║  Level 2 — j.u.c Framework  ║  46-105  ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 6-7            ║  Level 3 — Advanced         ║ 106-136  ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 7-8            ║  Level 4 — Spring Boot      ║ 137-166  ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  Week 9-12           ║  Level 5 — Interview Ready  ║ 167-215  ║
╠══════════════════════╬═════════════════════════════╬══════════╣
║  TOTAL               ║                             ║   215    ║
╚══════════════════════╩═════════════════════════════╩══════════╝
```