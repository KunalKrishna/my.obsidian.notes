
#### commands to push this notes

create a obsidian vault
go to the directory & run command `git init -b main`

```bash
git status
git add .
git commit -m "mesg"
(git remote add origin <REMOTE_REPOSITORY_URL> )
git push -u origin main # Push Your Local Code to GitHub
```

