
VM  = hardware level abstraction 

JVM = Class Loader + Memory Area + Execution Engine
JRE  = JVM + /library
JDK = JRE + Development kit(javac)

## Brief about VM
sdfdsf



## JVM, JRE, JDK

### JVM, JRE, JDK: Diff based on Use Case
JRE or JDK - JVM always there 
When is JRE only sufficient ? 
When is JDK necessity ? 

### JVM, JRE, JDK through Analogies

### JVM in Depth : The Three Tier JVM Architecture

### Garbage Collection